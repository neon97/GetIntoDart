void checkNull() {
  //! A question Mark (?) => tell the complier that the value will be null;
  // int? value;
  // print(value);
  //! A Exclamation Mark (!) => tell the complier that the value will not be null at any cost;
  // print(value!);
  //! A Doule Question Mark (??) => provides a back up data when the datatypes is not been initislaized / been null;
  // print(value??5);
}
